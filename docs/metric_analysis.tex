\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{array}

\graphicspath{{metric_analysis_files/}}
\sloppy

\title{FCFS Appointment Simulation: Behavior Sensitivity Report}
\author{CUIMC Appointment Simulation}
\date{\today}

\begin{document}
\maketitle

\section{Purpose and Main Questions}

This report studies the first appointment policy in the simulation: first-come, first-served (FCFS). The goal is to understand how patient behavior parameters affect clinic performance and whether one patient class is helped more than the other.

The report is organized by behavior, not by metric. For each behavior we ask:

\begin{enumerate}
    \item What parameter changes?
    \item What stays fixed?
    \item What happens overall?
    \item What happens to Class 1?
    \item What happens to Class 2?
    \item Is the effect mainly efficiency, redistribution, or both?
\end{enumerate}

The baseline uses \texttt{configs/baseline.yaml}: 32 slots per day, a 14-day booking horizon, 365 measured days, and two equal-size arrival streams with $\lambda_1 = 50$ and $\lambda_2 = 50$ arrivals per day. Both classes have the same baseline behavior parameters.

\section{Simple Metric Definitions}

The report uses rates and averages, not total counts. This keeps scenarios comparable even when simulation length or arrival volume changes.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{lp{4.6cm}p{8.2cm}}
\toprule
Plain name & Code name & Meaning \\
\midrule
Served rate, Class $i$ & \texttt{percent\_serviced\_i} & Share of Class $i$ arrivals that complete service. This is the main class-level access measure. \\
Overall served rate & \texttt{overall\_percent\_serviced} & Share of all arrivals that complete service. This is the main aggregate access measure. \\
Utilization & \texttt{average\_utilization} & Average share of daily slots that become completed visits. This is aggregate only; there is no class-specific utilization. \\
Accepted wait & \texttt{mean\_accepted\_booking\_delay} & Average booking delay among patients who accepted an appointment. This can be affected by selection because patients who balk are excluded. \\
Offered wait & \texttt{mean\_offered\_booking\_delay} & Average booking delay among patients who received an offer, including patients who balked. This is the better patient-facing wait measure. \\
Class gap & \texttt{percent\_serviced\_1 - percent\_serviced\_2} & Positive means Class 1 has higher served rate. Negative means Class 2 has higher served rate. \\
\bottomrule
\end{tabular}
}
\caption{Simple metric names used in the report.}
\end{table}

\section{Baseline Scenario}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{scenario_metric_comparison.png}
    \caption{Baseline and Scenario 2 comparison. The main analysis below focuses on the baseline FCFS model and changes one behavior family at a time.}
\end{figure}

\begin{table}[H]
\centering
\begin{tabular}{lrrrrrr}
\toprule
Scenario & Utilization & Overall served & Accepted wait & Offered wait & Class gap & Delay gap \\
\midrule
Baseline & 0.839 & 0.269 & 8.35 & 9.30 & 0.001 & 0.003 \\
Scenario 2 & 1.000 & 0.395 & 4.29 & 5.06 & -0.008 & 0.005 \\
\bottomrule
\end{tabular}
\caption{Scenario values are rates or averages. Class gap is Class 1 served rate minus Class 2 served rate.}
\end{table}

In the baseline, the two classes have the same arrival rate and behavior rules. Because of that, any large class difference in the sensitivity plots is caused by the parameter difference introduced in that plot.

\section{How to Read the Behavior Panels}

Most behavior sections use the same six-panel heatmap layout:

\begin{itemize}
    \item top row: overall served rate, Class 1 served rate, Class 2 served rate;
    \item bottom row: overall offered wait, Class 1 offered wait, Class 2 offered wait.
\end{itemize}

The x-axis is the Class 1 value of the behavior parameter. The y-axis is the Class 2 value. The diagonal is where both classes have the same value. Off the diagonal, one class has different behavior from the other.

This layout is meant to answer the practical question directly: does the parameter improve the whole system, or does it mostly move access from one class to the other?

\section{Balking}

Balking means a patient receives an offer but rejects it because the appointment is too far away. In this model, balking has two parts:

\begin{itemize}
    \item the \textbf{threshold}: the delay where high balking begins;
    \item the \textbf{step}: the high balking probability after that threshold.
\end{itemize}

A higher step means patients reject long-delay offers more often. A lower threshold means patients begin rejecting at shorter waits.

\subsection{Balking Step}

\textbf{What changes.} Class 1 and Class 2 balking step values vary from 0 to 1.

\textbf{What stays fixed.} Balking thresholds stay at the baseline value of 9 days. All no-show, cancellation, arrival, capacity, and horizon settings stay at baseline.

\textbf{What to look at.} The served-rate panels show who remains in the system. The offered-wait panels show whether more balking reduces the wait environment.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{balking_step_behavior_panels.png}
    \caption{Balking step sweep. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{balking_step_class1_service_heatmap.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{balking_step_class2_service_heatmap.png}
    \caption{Class-specific served-rate heatmaps for the balking step sweep. Left: Class 1. Right: Class 2.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{balking_class1_step_slice.png}
    \caption{One-dimensional slice: Class 1 balking step changes while Class 2 stays fixed at the baseline step, 0.50.}
\end{figure}

\textbf{Results.} When a class has a higher balking step, that class rejects more long-delay offers. Its served rate falls because more of its arrivals leave before booking. The other class can benefit because released slots become available to it. Offered wait can fall when balking becomes high enough, but lower wait is not always a pure improvement because some patients are leaving the system.

\textbf{Main takeaway.} Balking step is mainly a redistribution parameter. The class with the lower balking step is more likely to be served. Overall utilization often stays fairly stable because the clinic still has enough demand to fill slots.

\subsection{Balking Threshold}

\textbf{What changes.} Class 1 and Class 2 balking thresholds vary across the booking horizon.

\textbf{What stays fixed.} Balking step stays at the baseline value of 0.50. All other behavior and arrival settings stay at baseline.

\textbf{What to look at.} A lower threshold means patients start rejecting earlier. A higher threshold means patients tolerate longer waits before high balking begins.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{balking_threshold_behavior_panels.png}
    \caption{Balking threshold sweep. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{balking_threshold_class1_service_heatmap.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{balking_threshold_class2_service_heatmap.png}
    \caption{Class-specific served-rate heatmaps for the balking threshold sweep. Left: Class 1. Right: Class 2.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{balking_class1_threshold_slice.png}
    \caption{One-dimensional slice: Class 1 balking threshold changes while Class 2 stays fixed at the baseline threshold, 9 days.}
\end{figure}

\textbf{Results.} The class with the higher threshold tends to receive more completed service because its patients accept longer waits. The class with the lower threshold leaves the system earlier. Offered wait can rise when thresholds are high because more patients accept far-future appointments and keep the calendar congested.

\textbf{Main takeaway.} Balking threshold is also mainly redistributive. The class that tolerates longer waits is more likely to be served. A lower accepted wait can be misleading if it happens because many long-wait patients balked and disappeared from the accepted-wait calculation.

\subsection{Balking Threshold and Step Together}

\textbf{What changes.} Both classes receive the same balking threshold and the same balking step at each grid point.

\textbf{What stays fixed.} The two classes are treated identically in this sweep. Arrival rates, no-show, cancellation, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{balking_threshold_jump_heatmaps.png}
    \caption{Balking threshold and step when both classes share the same values.}
\end{figure}

\textbf{Results.} Because both classes use the same balking rule, there is almost no structural class advantage. The main effect is system-level: lower thresholds and higher steps remove more long-delay demand, while higher thresholds and lower steps keep more patients in the booking pool.

\textbf{Main takeaway.} Absolute balking level affects the access/wait tradeoff. Class advantage appears when the classes differ from each other.

\section{No-Show}

No-show behavior happens after booking. A patient has already accepted an appointment, but then may not attend. This means no-shows affect completed visits and utilization directly, but they do not strongly change the delay that was offered at booking time.

\subsection{No-Show Step}

\textbf{What changes.} Class 1 and Class 2 no-show step values vary from 0 to 1.

\textbf{What stays fixed.} No-show thresholds stay at the baseline value of 6 days. Balking, cancellation, arrival, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{no_show_step_behavior_panels.png}
    \caption{No-show step sweep. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\textbf{Results.} A higher no-show step means more booked patients fail to attend after the threshold. That lowers the served rate for the class with higher no-show risk and reduces aggregate utilization because booked slots are lost. Offered wait changes little because the no-show decision happens after the appointment was booked.

\textbf{Main takeaway.} No-show step has a strong efficiency effect and a class redistribution effect. It wastes capacity and benefits the class with lower no-show risk.

\subsection{No-Show Threshold}

\textbf{What changes.} Class 1 and Class 2 no-show thresholds vary across the booking horizon.

\textbf{What stays fixed.} No-show step stays fixed. Balking, cancellation, arrival, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{no_show_threshold_behavior_panels.png}
    \caption{No-show threshold sweep. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\textbf{Results.} A higher no-show threshold means no-show risk begins only at longer waits. That improves the served rate for that class and can improve overall utilization. Offered wait remains much less sensitive than served rate because no-show behavior occurs after booking.

\textbf{Main takeaway.} The class with the higher no-show threshold is more likely to complete service. No-show thresholds matter for completed visits, not for offered wait.

\subsection{No-Show Threshold and Step Together}

\textbf{What changes.} Both classes receive the same no-show threshold and the same no-show step at each grid point.

\textbf{What stays fixed.} The two classes are treated identically in this sweep. Balking, cancellation, arrival, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{no_show_threshold_jump_heatmaps.png}
    \caption{No-show threshold and step when both classes share the same values.}
\end{figure}

\textbf{Results.} High no-show step and low no-show threshold create many missed appointments. This lowers utilization and the overall served rate. Since both classes have the same no-show rule, there is little class advantage.

\textbf{Main takeaway.} Absolute no-show behavior is one of the clearest efficiency levers. Class advantage appears when one class has lower no-show risk than the other.

\section{Cancellation}

Cancellation is different from balking and no-show. It is not threshold-based in the current model. It is a daily probability that a future appointment is canceled before the service day.

\subsection{Cancellation Probability}

\textbf{What changes.} Class 1 and Class 2 cancellation probabilities vary from 0 to 0.30.

\textbf{What stays fixed.} Balking, no-show, arrival, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{cancellation_behavior_panels.png}
    \caption{Cancellation probability sweep. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\textbf{Results.} A class with higher cancellation probability loses more booked patients before service. Cancellations may free future capacity for someone else, but the canceling patient is still lost. This creates a large class served-rate gap when cancellation differs by class.

\textbf{Main takeaway.} Cancellation is a direct completion-risk parameter. It is one of the strongest drivers of class advantage because the class with lower cancellation probability is much more likely to complete service.

\section{Arrival Rate and Class Mix}

Arrival parameters control how much demand enters the system. They do not directly change patient behavior, but they change congestion.

\subsection{Total Arrival Rate and Class Mix}

\textbf{What changes.} Total arrivals per day and the Class 1 share of arrivals vary. The report uses actual arrival rates because that is easier to read than a normalized demand scale.

\textbf{What stays fixed.} Balking, no-show, cancellation, capacity, and horizon stay at baseline.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{arrival_mix_behavior_panels.png}
    \caption{Arrival rate and class mix. Top row shows served rates. Bottom row shows offered waits.}
\end{figure}

\textbf{Results.} Higher total arrival rate lowers the overall served rate and raises offered wait. Utilization can remain high even when access is poor because the clinic is busy but cannot serve a large share of arrivals. Changing the class mix has a weaker class-benefit effect when the two classes have the same behavior rules.

\textbf{Main takeaway.} Demand pressure is the main aggregate access driver. Class mix alone mostly changes volume unless the classes also differ in behavior.

\subsection{Changing One Class Arrival Rate}

\textbf{What changes.} One class arrival rate changes while the other class stays fixed.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{class_arrival_rate_benefit_lines.png}
    \caption{Changing one class arrival rate at a time.}
\end{figure}

\textbf{Results.} Increasing either class arrival rate increases congestion. Under FCFS and equal behavior rules, this does not create a strong persistent class advantage by itself.

\textbf{Main takeaway.} Own-class arrival volume matters for congestion, but class labels do not matter much unless class behavior differs.

\section{FCFS Stress Test}

The previous sections change behavior parameters. This section keeps behavior fixed and varies total demand to show how FCFS behaves under light load, baseline load, and overload.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{fcfs_capacity_stress_curves.png}
    \caption{FCFS stress curves as total arrivals per day change. The vertical dashed line marks the baseline total arrival rate.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{fcfs_arrival_outcome_decomposition.png}
    \caption{Arrival outcome decomposition under FCFS. Each band is a share of measured arrivals.}
\end{figure}

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{rrrrrrrrr}
\toprule
Total arrivals/day & Utilization & Overall served & Offered wait & Served & Balked & No offer & Lost after booking & Class gap \\
\midrule
30 & 0.943 & 0.998 & 0.22 & 0.998 & 0.000 & 0.000 & 0.002 & -0.000 \\
100 & 0.839 & 0.269 & 9.30 & 0.269 & 0.356 & 0.000 & 0.376 & 0.001 \\
170 & 0.841 & 0.157 & 10.59 & 0.157 & 0.242 & 0.346 & 0.255 & 0.001 \\
\bottomrule
\end{tabular}
}
\caption{Selected stress-test points. Outcome columns are shares of arrivals. The baseline demand level is 100 arrivals per day.}
\end{table}

\textbf{Results.} Under light load, almost every arrival is served. Near baseline, the clinic is busy, but only a minority of arrivals complete service. Under overload, utilization still looks high, but the served rate falls further and no-offers become a major loss channel.

\textbf{Main takeaway.} Utilization alone is not enough. A clinic can be busy and still have poor access. Served rate and outcome decomposition are needed to understand FCFS performance.

\section{Regression Screening}

The heatmaps change one or two parameters at a time. The regression screen asks a broader question: across many random parameter settings, which inputs best explain the outputs?

\textbf{What changes.} 240 random FCFS parameter settings were sampled. Each setting was run with 2 random seeds and averaged.

\textbf{How to read it.} The regression coefficients are standardized. A larger absolute coefficient means that input has a stronger average association with the target metric over the sampled range.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{regression_standardized_coefficients.png}
    \caption{Largest standardized regression coefficients from randomized FCFS simulations. Positive coefficients increase the target metric; negative coefficients decrease it.}
\end{figure}

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{llr}
\toprule
Target & Most important feature & Standardized coefficient \\
\midrule
Utilization & Average no-show threshold & 0.512 \\
Utilization & Average no-show step & -0.423 \\
Utilization & Average cancellation probability & 0.320 \\
\addlinespace
Overall served rate & Total arrival rate & -0.774 \\
Overall served rate & Average no-show threshold & 0.251 \\
Overall served rate & Average no-show step & -0.175 \\
\addlinespace
Offered wait & Total arrival rate & 0.576 \\
Offered wait & Average cancellation probability & -0.441 \\
Offered wait & Average balking threshold & 0.278 \\
\addlinespace
Class gap & Cancellation probability gap & -0.452 \\
Class gap & Balking threshold gap & 0.429 \\
Class gap & Balking step gap & -0.349 \\
\bottomrule
\end{tabular}
}
\caption{Top regression drivers. Gap means Class 1 value minus Class 2 value.}
\end{table}

\textbf{Results.} Aggregate performance is mostly explained by total arrival rate and average no-show behavior. Class gap is mostly explained by differences between the classes: cancellation gap, balking threshold gap, and balking step gap.

\textbf{Main takeaway.} The regression confirms the heatmaps. Absolute demand and no-show behavior explain overall performance. Differences between class behavior explain who benefits.

\section{Final Takeaways}

\begin{enumerate}
    \item \textbf{Served rate is the main access metric.} Utilization can stay high even when many patients are not served.
    \item \textbf{Balking mainly redistributes access.} The class that balks less, or tolerates longer waits, is more likely to be served.
    \item \textbf{No-show behavior is a strong efficiency driver.} No-shows waste booked slots and lower completed visits.
    \item \textbf{Cancellation differences create large class gaps.} A class with lower cancellation probability is much more likely to complete service.
    \item \textbf{Arrival rate controls congestion.} Higher demand lowers the overall served rate and increases offered wait.
    \item \textbf{Class mix alone is weak unless behavior differs.} FCFS does not strongly privilege a class label by itself.
    \item \textbf{For class benefit, gaps matter more than absolute levels.} If both classes share the same behavior rule, class advantage stays near zero. If one class has a more favorable rule, that class benefits.
\end{enumerate}

\end{document}
