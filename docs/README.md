# Documentation Sources

- `reports/metric_analysis/`: canonical metric-focused study source, figures, data, rendered thorough report, and research-style variants.
- `companions/`: narrower companion extracts that point back to the canonical report.
- `reference/`: simulation model and code-path documentation, with rendered HTML under `reference/simulation_explanation/`.
- `archive/`: superseded source notes kept for traceability.

Metric report figures, CSVs, manifests, rendered PDF/TeX/Markdown, and
research-style variants live under `reports/metric_analysis/`.

Companion report outputs and parameter-sweep outputs still live under
`../outputs/`.
